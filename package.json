{
  "name": "root",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "extension",
    "server"
  ],
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n ext,server -c blue,green \"cd extension && node_modules/.bin/wxt\" \"cd server && node_modules/.bin/wrangler dev\"",
    "dev:ext": "cd extension && node_modules/.bin/wxt",
    "dev:server": "cd server && node_modules/.bin/wrangler dev"
  },
  "devDependencies": {
    "concurrently": "^9"
  },
  "overrides": {
    "@vitejs/plugin-react": "4"
  }
}
