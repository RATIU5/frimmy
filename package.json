{
  "name": "root",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "extension",
    "server"
  ],
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n ext,server -c blue,green \"cd extension && wxt\" \"cd server && wrangler dev\"",
    "dev:ext": "cd extension && wxt",
    "dev:server": "cd server && wrangler dev"
  },
  "devDependencies": {
    "concurrently": "^9"
  },
  "overrides": {
    "@vitejs/plugin-react": "4"
  }
}
