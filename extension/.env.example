# Copy to .env and fill in. WXT inlines WXT_* vars at build time.
# Auth is the user's Cloudflare Access login (CF_Authorization cookie on this host),
# so no secrets live in the bundle — just the API origin.
WXT_API_URL=https://server.YOUR-SUBDOMAIN.workers.dev
