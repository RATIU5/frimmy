# Frimmy

A browser extension using WXT that allows a user to select a part of a web page with js-element-picker and modify that section of a webpage using natural language. Users can then share their edits with other users by sharing a unique URL so they can visualize their edits. All the infrastructure will be provided by Cloudflare: Workers for routing, Workers KV for storing code diffs, Workers AI for handling user queries.
